package com.example.sms.configuration;

import org.springframework.context.annotation.Configuration;
import org.springframework.web.servlet.config.annotation.WebMvcConfigurer;

@Configuration
public class SupplierConfig implements WebMvcConfigurer {
//    @Override
//    public void addCorsMappings(CorsRegistry registry) {
//        registry.addMapping("/api/v1/suppliers")
//                .allowedOrigins("http://localhost:8080") // Adjust based on your frontend's address
//                .allowedMethods("GET", "POST");
//    }
}
